import sqlite3 from "sqlite3";
import { open } from "sqlite";

export async function openDb() {
  return open({
    filename: "./credentials.db", // <- Current folder me create hoga
    driver: sqlite3.Database
  });
}
